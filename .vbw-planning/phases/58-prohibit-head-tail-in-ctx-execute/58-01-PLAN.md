---
phase: 58
plan: "01"
title: "Add Anti-patterns section to ctx-rules.md"
wave: 1
depends_on: []
must_haves:
  - "### Anti-patterns section added after ### Prefer in rules/ctx-rules.md"
  - "### Anti-patterns section added after ### Prefer in .claude/rules/ctx-rules.md"
  - "Section accurately explains why head/tail inside ctx_execute is harmful"
  - "Section prescribes programmatic analysis as the correct replacement pattern"
---

## Task P01: Add Anti-patterns section to ctx-rules.md source and installed copy

**Files to modify:**
- `rules/ctx-rules.md` — the source file
- `.claude/rules/ctx-rules.md` — the installed file Claude Code reads at session start

**What to add** (after the `### Prefer` section in each file):

```
### Anti-patterns

- **Never use `head`/`tail` inside `ctx_execute` to cap output.** Inside the sandbox, all stdout is captured for FTS5 indexing — there is no context-size penalty for full output. Truncating inside the sandbox discards data *before* it can be indexed or searched. Write programmatic analysis instead: filter, count, extract, then print only the findings.
```

**Acceptance criteria:**
- Both files have the Anti-patterns section
- The section appears after the Prefer section (at end of file)
- No other content is changed
