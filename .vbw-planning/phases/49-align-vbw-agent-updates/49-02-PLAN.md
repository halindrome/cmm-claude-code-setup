---
phase: 49
plan: 02
title: Sync vbw-dev.md body and add skill_no_activation to remaining agents
type: execute
wave: 1
depends_on: []
cross_phase_deps: []
autonomous: true
effort_override: thorough
skills_used: []
files_modified:
  - agents/vbw-dev.md
  - agents/vbw-scout.md
  - agents/vbw-lead.md
  - agents/vbw-debugger.md
  - agents/vbw-docs.md
forbidden_commands:
  - scripts/bump-version.sh
  - git push
must_haves:
  truths:
    - "vbw-dev.md body handles `<skill_no_activation>` orchestrator signal"
    - "vbw-dev.md DEVN-05 requires `pre_existing_issues` as JSON array in SUMMARY.md frontmatter (not prose)"
    - "vbw-dev.md Stage 3 (Produce Summary) includes `ac_results` when plan has must_haves and always emits `pre_existing_issues: []` when none"
    - "vbw-dev.md contains a `## Tool blocks` section that documents following `REPLACE WITH:` instructions from hook messages"
    - "vbw-dev.md preserves the CMM `## Context Mode Web Fetch` section (intentional CMM extension)"
    - "vbw-dev.md preserves CMM `hooks:` frontmatter block and MAINTENANCE override comment"
    - "vbw-scout.md, vbw-lead.md, vbw-debugger.md, vbw-docs.md each handle `<skill_no_activation>` orchestrator signal"
    - "vbw-debugger.md override comment explicitly notes `Task(vbw-debugger)` is a CMM-only addition not present in VBW source"
  artifacts:
    - {path: "agents/vbw-dev.md", provides: "Dev agent aligned to VBW v1.35.0 with CMM extensions preserved", contains: "skill_no_activation"}
    - {path: "agents/vbw-dev.md", provides: "DEVN-05 structured pre-existing issues output", contains: "pre_existing_issues"}
    - {path: "agents/vbw-dev.md", provides: "Tool blocks REPLACE WITH protocol (load-bearing for cmm-grep-nudge.sh)", contains: "Tool blocks"}
    - {path: "agents/vbw-dev.md", provides: "Context Mode Web Fetch extension preserved", contains: "Context Mode Web Fetch"}
    - {path: "agents/vbw-scout.md", provides: "skill_no_activation handling", contains: "skill_no_activation"}
    - {path: "agents/vbw-lead.md", provides: "skill_no_activation handling", contains: "skill_no_activation"}
    - {path: "agents/vbw-debugger.md", provides: "skill_no_activation handling + Task(vbw-debugger) comment", contains: "skill_no_activation"}
    - {path: "agents/vbw-docs.md", provides: "skill_no_activation handling", contains: "skill_no_activation"}
  key_links:
    - {from: "agents/vbw-dev.md `## Tool blocks`", to: "hooks/project/cmm-grep-nudge.sh REPLACE WITH output", via: "protocol-documentation"}
    - {from: "agents/vbw-dev.md DEVN-05", to: "agents/vbw-qa.md Pre-Existing Failure Handling (plan 01)", via: "pre_existing_issues-schema"}
---
<objective>
Bring all remaining agent files (`vbw-dev.md` plus the four lower-drift agents `vbw-scout.md`, `vbw-lead.md`, `vbw-debugger.md`, `vbw-docs.md`) to VBW v1.35.0 parity while preserving CMM-specific extensions. `vbw-dev.md` receives the heaviest edits: `<skill_no_activation>` handling, DEVN-05 structured `pre_existing_issues` rule, Stage 3 `ac_results` + `pre_existing_issues: []` emission rule, and a new `## Tool blocks` section that documents the `REPLACE WITH:` protocol used by `hooks/project/cmm-grep-nudge.sh`. The four other agents receive the two-line `<skill_no_activation>` branch in their Skill Activation section; `vbw-debugger.md` also gets an override-comment note about its CMM-only `Task(vbw-debugger)` tool entry. This plan is file-disjoint from plan 01 (only touches the five agent files listed above).
</objective>
<context>
@.vbw-planning/phases/49-align-vbw-agent-updates/49-RESEARCH.md
@/Users/ahby/Sources/vibe-better-with-claude-code-vbw/agents/vbw-dev.md
@/Users/ahby/Sources/vibe-better-with-claude-code-vbw/agents/vbw-scout.md
@/Users/ahby/Sources/vibe-better-with-claude-code-vbw/agents/vbw-lead.md
@/Users/ahby/Sources/vibe-better-with-claude-code-vbw/agents/vbw-debugger.md
@/Users/ahby/Sources/vibe-better-with-claude-code-vbw/agents/vbw-docs.md
@agents/vbw-dev.md
@agents/vbw-scout.md
@agents/vbw-lead.md
@agents/vbw-debugger.md
@agents/vbw-docs.md
@hooks/project/cmm-grep-nudge.sh
Research section "Breaking Changes" items 1, 2, 6, 8 and "Recommended Phase 48 Scope" Priority 1 (vbw-dev body sync) + Priority 2 (skill_no_activation across 6 agents) + Priority 3 (debugger Task(vbw-debugger) documentation) define the scope. The `## Tool blocks` section in vbw-dev is load-bearing for phase 46 behavior (`cmm-grep-nudge.sh` emits `REPLACE WITH:` directives that dev agents must follow).
</context>
<tasks>
<task type="auto">
  <name>Sync vbw-dev.md body to VBW v1.35.0 (preserve CMM extensions)</name>
  <files>
    agents/vbw-dev.md
  </files>
  <action>
Targeted edits (not a full-file replace — CMM extensions must be preserved):
  1. Add `<skill_no_activation>` handling to the Skill Activation / Stage 1 section (mirror VBW source `/Users/ahby/Sources/vibe-better-with-claude-code-vbw/agents/vbw-dev.md` lines 17–18).
  2. Replace the DEVN-05 rule so it requires `pre_existing_issues` as a JSON array in `SUMMARY.md` frontmatter (per VBW source lines 47–50), not prose-only.
  3. Update Stage 3 (Produce Summary) to add: `if plan has must_haves, emit ac_results per template (pass/fail/partial); omit otherwise` AND `Always emit pre_existing_issues: [] in SUMMARY frontmatter when no DEVN-05 issues were found`.
  4. Append a `## Tool blocks` section (mirror VBW source lines 147–156): documents that when a PreToolUse hook (e.g. `cmm-grep-nudge.sh`, `cmm-nudge.sh`) emits a `REPLACE WITH:` directive, the agent must follow the replacement instruction instead of retrying the blocked call.
  5. Update the `## Blocked Task Self-Start` section wording to match VBW source.
  6. PRESERVE: the CMM `## Context Mode Web Fetch` block, the CMM `hooks:` frontmatter block, and the MAINTENANCE override comment.
Use Read with offset/limit to target the specific sections, then Edit for surgical changes.
  </action>
  <verify>
Each of these greps must return ≥1 match against `agents/vbw-dev.md`:
  - `grep -n 'skill_no_activation' agents/vbw-dev.md`
  - `grep -n 'pre_existing_issues' agents/vbw-dev.md` (should appear in DEVN-05 section and Stage 3 section)
  - `grep -n 'ac_results' agents/vbw-dev.md`
  - `grep -n '## Tool blocks' agents/vbw-dev.md`
  - `grep -n 'REPLACE WITH' agents/vbw-dev.md`
  - `grep -n 'Context Mode Web Fetch' agents/vbw-dev.md` (CMM extension preserved)
  - `grep -n 'MAINTENANCE' agents/vbw-dev.md` (override comment preserved)
  - `grep -n '^hooks:' agents/vbw-dev.md` (hooks block preserved)
  </verify>
  <done>
All eight greps match; no unintended removal of CMM extensions.
  </done>
</task>
<task type="auto">
  <name>Add skill_no_activation handling to vbw-scout.md</name>
  <files>
    agents/vbw-scout.md
  </files>
  <action>
In the Skill Activation section, add the `<skill_no_activation>` branch from VBW source `/Users/ahby/Sources/vibe-better-with-claude-code-vbw/agents/vbw-scout.md` (~line 17). Preserve all CMM-specific content (hooks block, override comment, any CMM-added sections). Use Read + Edit — do NOT full-file replace.
  </action>
  <verify>
`grep -n 'skill_no_activation' agents/vbw-scout.md` returns ≥1 match. `grep -n 'MAINTENANCE' agents/vbw-scout.md` still matches (override comment preserved). `grep -n '^hooks:' agents/vbw-scout.md` still matches.
  </verify>
  <done>
Scout acknowledges `<skill_no_activation>` tag.
  </done>
</task>
<task type="auto">
  <name>Add skill_no_activation handling to vbw-lead.md and vbw-docs.md</name>
  <files>
    agents/vbw-lead.md
    agents/vbw-docs.md
  </files>
  <action>
For each of `agents/vbw-lead.md` and `agents/vbw-docs.md`: in the Skill Activation section, add the `<skill_no_activation>` branch mirrored from the corresponding VBW source file. Preserve all CMM-specific content. Use Read + Edit per file.
  </action>
  <verify>
`grep -n 'skill_no_activation' agents/vbw-lead.md` returns ≥1 match. `grep -n 'skill_no_activation' agents/vbw-docs.md` returns ≥1 match. Override comments and hooks blocks preserved in both files.
  </verify>
  <done>
Both lead and docs agents acknowledge `<skill_no_activation>`.
  </done>
</task>
<task type="auto">
  <name>Update vbw-debugger.md: skill_no_activation + override-comment note</name>
  <files>
    agents/vbw-debugger.md
  </files>
  <action>
Two edits:
  1. Add `<skill_no_activation>` branch in the Skill Activation section (mirror VBW source).
  2. In the MAINTENANCE override comment block at the top of the file, add a line explicitly documenting: "CMM EXTENSION: `tools:` frontmatter in this override adds `Task(vbw-debugger)` (self-spawn capability) NOT present in VBW source. Reason: CMM debug loops reuse debugger recursively; do not remove during future VBW syncs."
Do NOT change the actual `tools:` allowlist — it stays as-is (this plan only documents it). Do NOT add or remove any other frontmatter field.
  </action>
  <verify>
`grep -n 'skill_no_activation' agents/vbw-debugger.md` returns ≥1 match. `grep -n 'CMM EXTENSION' agents/vbw-debugger.md` returns ≥1 match. `grep -n 'Task(vbw-debugger)' agents/vbw-debugger.md` returns ≥1 match in the `tools:` frontmatter line AND ≥1 in the comment block.
  </verify>
  <done>
Debugger acknowledges `<skill_no_activation>`; override comment documents the CMM-only `Task(vbw-debugger)` addition.
  </done>
</task>
<task type="auto">
  <name>Commit agent sync as a single atomic commit</name>
  <files>
    agents/vbw-dev.md
    agents/vbw-scout.md
    agents/vbw-lead.md
    agents/vbw-debugger.md
    agents/vbw-docs.md
  </files>
  <action>
Stage exactly these five files and commit with `feat(49-02): sync vbw-dev body + skill_no_activation across remaining agents`. Do NOT stage any file outside `agents/`. Do NOT touch `CHECKSUMS.sha256` — plan 03 regenerates it.
  </action>
  <verify>
`git log -1 --name-only` shows exactly the five agent files (no other paths). `git status --short` shows no staged agent changes remaining. No file under `hooks/`, `scripts/`, `tests/`, or `CHECKSUMS.sha256` is in the HEAD commit.
  </verify>
  <done>
One commit; five-file scope.
  </done>
</task>
</tasks>
<verification>
1. `grep -l 'skill_no_activation' agents/vbw-dev.md agents/vbw-scout.md agents/vbw-lead.md agents/vbw-debugger.md agents/vbw-docs.md | wc -l` returns 5.
2. `grep -n '## Tool blocks' agents/vbw-dev.md` returns ≥1 match.
3. `grep -n 'ac_results\|pre_existing_issues' agents/vbw-dev.md` returns ≥2 matches.
4. `grep -n 'Context Mode Web Fetch' agents/vbw-dev.md` returns ≥1 match (CMM extension preserved).
5. `grep -n 'CMM EXTENSION' agents/vbw-debugger.md` returns ≥1 match (Task self-spawn documented).
6. `grep -n '^hooks:' agents/vbw-dev.md agents/vbw-scout.md agents/vbw-lead.md agents/vbw-debugger.md agents/vbw-docs.md` returns a match in each file (CMM hooks blocks preserved).
7. `git log -1 --name-only` lists exactly the five agent files in plan-02's HEAD commit.
</verification>
<success_criteria>
- `agents/vbw-dev.md` matches VBW v1.35.0 behaviorally (all five targeted additions present) while retaining CMM extensions (`## Context Mode Web Fetch`, hooks block, override comment).
- All five agent files handle `<skill_no_activation>`.
- `agents/vbw-debugger.md` override comment documents the CMM-only `Task(vbw-debugger)` tool entry.
- One atomic commit under `feat(49-02):` scope covering exactly the five agent files.
</success_criteria>
<output>
02-SUMMARY.md
</output>
