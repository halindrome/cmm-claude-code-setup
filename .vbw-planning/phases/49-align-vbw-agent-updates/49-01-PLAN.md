---
phase: 49
plan: 01
title: Sync vbw-qa.md body and revert frontmatter to VBW v1.35.0
type: execute
wave: 1
depends_on: []
cross_phase_deps: []
autonomous: true
effort_override: thorough
skills_used: []
files_modified:
  - agents/vbw-qa.md
forbidden_commands:
  - scripts/bump-version.sh
  - git push
must_haves:
  truths:
    - "vbw-qa.md frontmatter uses `disallowedTools: Task` only; no `tools:` allowlist present"
    - "vbw-qa.md body contains the NON-NEGOTIABLE write-verification.sh persistence gate wording from VBW source"
    - "vbw-qa.md body contains `plan_ref` and `plans_verified` requirements in the Communication section"
    - "vbw-qa.md body contains a `## Debug Session QA Mode` section"
    - "vbw-qa.md body contains a `## Remediation Round Verification Scope` section"
    - "vbw-qa.md body contains a `## Pre-Existing Failure Handling` section with `pre_existing_issues` JSON array in `qa_verdict`"
    - "vbw-qa.md body handles the `<skill_no_activation>` orchestrator signal"
    - "CMM `hooks:` frontmatter block (PreToolUse/PostToolUse) is preserved"
    - "The CMM override comment block at the top of the file is preserved (MAINTENANCE note)"
  artifacts:
    - {path: "agents/vbw-qa.md", provides: "QA agent override aligned to VBW v1.35.0", contains: "write-verification.sh"}
    - {path: "agents/vbw-qa.md", provides: "Debug session mode handling", contains: "Debug Session QA Mode"}
    - {path: "agents/vbw-qa.md", provides: "Pre-existing failure structured output", contains: "pre_existing_issues"}
    - {path: "agents/vbw-qa.md", provides: "skill_no_activation handling", contains: "skill_no_activation"}
  key_links:
    - {from: "agents/vbw-qa.md frontmatter", to: "VBW source /Users/ahby/Sources/vibe-better-with-claude-code-vbw/agents/vbw-qa.md", via: "disallowedTools-match"}
    - {from: "agents/vbw-qa.md body", to: "hooks/project/write-verification.sh semantics (documented in VBW source)", via: "persistence-gate"}
---
<objective>
Align `agents/vbw-qa.md` with VBW v1.35.0 source behavior. The CMM override currently lacks the `write-verification.sh` persistence gate, `plan_ref`/`plans_verified` validation requirements, `Debug Session QA Mode`, `Remediation Round Verification Scope`, expanded deviation rules, and `Pre-Existing Failure Handling` — all of which are load-bearing for VBW-orchestrated QA rounds. Additionally, the CMM override added a `tools:` allowlist not present in VBW source; this must be reverted to `disallowedTools: Task` to avoid silently blocking future VBW capability additions. Preserve CMM-specific additions (`hooks:` frontmatter block, override comment). This plan touches `agents/vbw-qa.md` only; no shared files.
</objective>
<context>
@.vbw-planning/phases/49-align-vbw-agent-updates/49-RESEARCH.md
@/Users/ahby/Sources/vibe-better-with-claude-code-vbw/agents/vbw-qa.md
@agents/vbw-qa.md
Research sections "Breaking Changes For This Tool" items 3, 4, 5, 7 and "Recommended Phase 48 Scope" Priority 1 (vbw-qa sync) and Priority 2 (frontmatter revert) define the exact scope. VBW source is the authority for body content; CMM `hooks:` block and override comment are preserved verbatim.
</context>
<tasks>
<task type="auto">
  <name>Revert vbw-qa frontmatter: drop tools allowlist</name>
  <files>
    agents/vbw-qa.md
  </files>
  <action>
Read VBW source frontmatter from `/Users/ahby/Sources/vibe-better-with-claude-code-vbw/agents/vbw-qa.md` (first ~15 lines). Remove the `tools: Read, Grep, Glob, Bash, LSP, Skill` line from the CMM override frontmatter. Keep `disallowedTools: Task`, `permissionMode: plan`, `model`, `memory`, `name`, `description`, and the `hooks:` block (PreToolUse/PostToolUse entries for CMM-specific hooks). Do not change the `hooks:` block — it is a CMM extension.
  </action>
  <verify>
`grep -n '^tools:' agents/vbw-qa.md` returns nothing. `grep -n 'disallowedTools: Task' agents/vbw-qa.md` returns one match. `grep -n 'hooks:' agents/vbw-qa.md` still returns one match (hooks block preserved).
  </verify>
  <done>
Frontmatter matches VBW source field-for-field except for the added `hooks:` block and CMM `memory` value (if it differs).
  </done>
</task>
<task type="auto">
  <name>Sync vbw-qa body: replace outdated sections with VBW v1.35.0 content</name>
  <files>
    agents/vbw-qa.md
  </files>
  <action>
Replace the body (everything after frontmatter) with the VBW v1.35.0 source body content from `/Users/ahby/Sources/vibe-better-with-claude-code-vbw/agents/vbw-qa.md`. The following sections MUST appear in the new body: the `write-verification.sh` NON-NEGOTIABLE persistence gate paragraph; `plan_ref` + `plans_verified` requirements in the `## Communication` section; `## Debug Session QA Mode` section; `## Remediation Round Verification Scope` section; expanded deviation handling (multi-item lines, plans-with-no-declared-deviations, known-issues during remediation, `pre_existing_issues` array in `qa_verdict` payload); `## Pre-Existing Failure Handling` section. Preserve the existing CMM override comment block at the top (above frontmatter or as a leading HTML comment — whichever the file uses). Do NOT remove or alter CMM-specific content in the comment block.
  </action>
  <verify>
Run these greps against `agents/vbw-qa.md` — each must return ≥1 match:
  - `grep -n 'write-verification.sh' agents/vbw-qa.md`
  - `grep -n 'plan_ref' agents/vbw-qa.md`
  - `grep -n 'plans_verified' agents/vbw-qa.md`
  - `grep -n 'Debug Session QA Mode' agents/vbw-qa.md`
  - `grep -n 'Remediation Round Verification Scope' agents/vbw-qa.md`
  - `grep -n 'Pre-Existing Failure Handling' agents/vbw-qa.md`
  - `grep -n 'pre_existing_issues' agents/vbw-qa.md`
  - `grep -n 'MAINTENANCE' agents/vbw-qa.md` (CMM override comment preserved)
Spot-check via `diff <(sed -n '/^---$/,/^---$/!p' /Users/ahby/Sources/vibe-better-with-claude-code-vbw/agents/vbw-qa.md) <(sed -n '/^---$/,/^---$/!p' agents/vbw-qa.md)` — differences should be limited to the CMM override comment.
  </verify>
  <done>
All seven greps above return matches; override comment preserved.
  </done>
</task>
<task type="auto">
  <name>Add skill_no_activation handling to vbw-qa body</name>
  <files>
    agents/vbw-qa.md
  </files>
  <action>
If the body sync in Task 2 already brought in VBW source `<skill_no_activation>` handling, verify it is present and skip. Otherwise, in the `## Skill Activation` (or `Stage 1 Skill Activation`) section, add the VBW source block that handles a `<skill_no_activation>` prompt tag (mirrors the existing `<skill_activation>` branch — acknowledges the tag and skips scanning `<available_skills>`).
  </action>
  <verify>
`grep -n 'skill_no_activation' agents/vbw-qa.md` returns ≥1 match. The wording matches VBW source `/Users/ahby/Sources/vibe-better-with-claude-code-vbw/agents/vbw-qa.md` skill-activation block.
  </verify>
  <done>
`skill_no_activation` tag is acknowledged in the agent body.
  </done>
</task>
<task type="auto">
  <name>Validate and commit vbw-qa.md sync</name>
  <files>
    agents/vbw-qa.md
  </files>
  <action>
Confirm the frontmatter YAML still parses (first `---...---` block is valid YAML — check field keys are correctly quoted/unquoted as VBW source does). Confirm the `hooks:` block and CMM override comment are intact. Stage only `agents/vbw-qa.md` and commit with message `feat(49-01): sync vbw-qa.md body + frontmatter to VBW v1.35.0`.
  </action>
  <verify>
`git status --short` shows only `agents/vbw-qa.md` modified (and nothing else staged in this commit). `git log -1 --pretty=%B` matches the commit message above. No other files touched.
  </verify>
  <done>
One atomic commit landed; vbw-qa.md is the only changed file.
  </done>
</task>
</tasks>
<verification>
1. `grep -c 'write-verification.sh\|plan_ref\|plans_verified\|Debug Session QA Mode\|Remediation Round Verification Scope\|Pre-Existing Failure Handling\|pre_existing_issues\|skill_no_activation' agents/vbw-qa.md` returns ≥8.
2. `grep -n '^tools:' agents/vbw-qa.md` returns nothing (allowlist reverted).
3. `grep -n 'disallowedTools: Task' agents/vbw-qa.md` returns one match.
4. `grep -n 'hooks:' agents/vbw-qa.md` returns one match (CMM extension preserved).
5. `grep -n 'MAINTENANCE' agents/vbw-qa.md` returns ≥1 match (override comment preserved).
6. `git log -1 --name-only` shows `agents/vbw-qa.md` as the sole file in the HEAD commit for this plan.
</verification>
<success_criteria>
- vbw-qa.md body is functionally equivalent to VBW v1.35.0 source (all eight targeted sections present).
- vbw-qa.md frontmatter uses `disallowedTools: Task` only; no CMM-added `tools:` allowlist.
- CMM `hooks:` frontmatter block and MAINTENANCE override comment preserved verbatim.
- One atomic commit authored under `feat(49-01):` scope; only `agents/vbw-qa.md` modified.
</success_criteria>
<output>
01-SUMMARY.md
</output>
