---
phase: 47
plan: 03
title: "ctx-annotate-nudge PostToolUse additionalContext hook + ctx-search-nudge retirement (Finding D + Phase 45 consolidation)"
wave: 1
depends_on: []
files_modified:
  - hooks/global/ctx-annotate-nudge.sh
  - hooks/global/ctx-search-nudge.sh
  - tests/test-ctx-annotate-nudge.sh
  - tests/test-ctx-search-nudge.sh
skills_used:
  - codebase-memory-exploring
  - codebase-memory-tracing
  - simplify
must_haves:
  truths:
    - "New hook hooks/global/ctx-annotate-nudge.sh: PostToolUse matcher mcp__context-mode__ctx_(execute|search|index|fetch_and_index). On qualifying calls, emits a single-line stdout JSON envelope {\"hookSpecificOutput\":{\"hookEventName\":\"PostToolUse\",\"additionalContext\":\"<msg>\"}} and exits 0. Does NOT use stderr. Does NOT emit decision:block."
    - "JSON escaping uses python3 -c 'import json; print(json.dumps({...}))' — NOT the incomplete sed 's/\"/\\\\\"/g' pattern. The hook pipes the full envelope dict through json.dumps so double quotes, backslashes, newlines, and control characters are all escaped correctly (research Finding D risk #3)."
    - "Cooldown: 120s via /tmp/ctx-nudge-${PROJECT_HASH} sentinel. Sentinel mtime check uses macOS/Linux-compatible fallback: stat -f %m \"$SENTINEL\" 2>/dev/null || stat -c %Y \"$SENTINEL\". If mtime < 120s, exit 0 without emitting additionalContext. On successful emit, touch the sentinel. If PROJECT_HASH unresolvable, fail-open (emit without cooldown — better to over-nudge than fail silently)."
    - "Message content (architect voice, terse, imperative, covers BOTH Phase 45 retrieval-protocol and the user's summarize-before-next-call instruction): 'STOP. Before your next tool call, state in ONE sentence what this ctx_* result told you. If it already answers the user, answer directly — do NOT run another ctx_* call. For a topic you have indexed this session, try ctx_search(queries=[...]) before re-running ctx_execute / ctx_fetch_and_index.' Exact wording may be refined during implementation but must contain the three load-bearing phrases: 'state in ONE sentence', 'do NOT run another ctx_* call', 'try ctx_search'."
    - "Matcher expands to include ctx_search (Phase 45's matcher did not) but remains tight enough to exclude ctx_stats and ctx_batch_execute (per 47-CONTEXT.md binding: existing matcher pattern minus batch_execute, plus ctx_search)."
    - "Bypass marker: if ctx call's tool_input JSON (serialized) contains literal '# ctx-exempt', exit 0 without emitting. Mirrors hooks/project/ctx-execute-enforcer.sh:111 convention."
    - "Fail-open on malformed JSON, missing tool_name, unknown tool_name, empty stdin, and python3 unavailable (matches ctx-search-nudge.sh pattern). Every parse failure exits 0 silently."
    - "hooks/global/ctx-search-nudge.sh is DELETED from the repo (git rm). tests/test-ctx-search-nudge.sh is also deleted. The deletion is atomic with the new hook creation in this plan. rules/project-settings-example.json and setup.sh wiring changes happen in Plan 05 (wave 2)."
  artifacts:
    - "hooks/global/ctx-annotate-nudge.sh: new file, chmod +x, starts with #!/bin/bash, one-line purpose comment, install/register header block naming PostToolUse matcher regex. Body: stdin parse via python3 (same base64 field-extraction pattern as ctx-search-nudge.sh to tolerate embedded newlines in intent fields), tool_name match, cooldown check, emit via python3 json.dumps, touch sentinel. Under 120 lines including comments."
    - "hooks/global/ctx-search-nudge.sh: deleted."
    - "tests/test-ctx-search-nudge.sh: deleted."
    - "tests/test-ctx-annotate-nudge.sh: new file, chmod +x, plain-bash PASS/FAIL counter matching tests/test-ctx-execute-cmm-nudge.sh shape. Covers 8 cases: (1) ctx_execute with intent → emits additionalContext JSON envelope on stdout, exit 0; (2) ctx_search → emits; (3) ctx_index → emits; (4) ctx_fetch_and_index → emits; (5) ctx_stats → exit 0 silent, no stdout JSON; (6) ctx_batch_execute → exit 0 silent, no stdout JSON (note: batch_execute was in Phase 45 matcher, but 47-CONTEXT.md aligns the new matcher to search+execute+index+fetch_and_index only per Finding D §(4)); (7) second ctx_execute within 120s of first → exit 0 silent (cooldown); (8) JSON envelope contains literal `\\n` and `\\\"` correctly escaped when the intent contains quotes and newlines. Also (9): malformed JSON on stdin → exit 0."
  key_links:
    - "Phase 45 predecessor: retires hooks/global/ctx-search-nudge.sh (stderr-only). Combined message must cover the retrieval-protocol content of the retired hook."
    - "Prior art: hooks/project/subagent-cmm-startup.sh lines 55–56 (SubagentStart additionalContext) — same JSON envelope shape, but that hook uses the incomplete sed escape; THIS plan must use python3 json.dumps instead."
    - "47-CONTEXT.md §Phase 45 disposition: 'Consolidate' — file deletions are part of this plan's atomic change."
    - "Coordination: Plan 01 creates /tmp/cmm-recent-*; this plan creates /tmp/ctx-nudge-*; disjoint key spaces, no collision."
---

## Objective
Replace `hooks/global/ctx-search-nudge.sh` (stderr advisory) with a new `hooks/global/ctx-annotate-nudge.sh` that uses the `hookSpecificOutput.additionalContext` PostToolUse channel (reaches Claude's next-turn reasoning verbatim). Expand the matcher to include `ctx_search`. Enforce a 120s cooldown via `/tmp/ctx-nudge-${PROJECT_HASH}`. Use `python3 json.dumps` for correct escaping. Delete the old hook and its test.

## Context
- @hooks/global/ctx-search-nudge.sh — existing stderr-only hook being retired; copy its stdin parse / base64 field extraction as a reliability pattern
- @hooks/project/subagent-cmm-startup.sh — prior art for additionalContext JSON envelope (SubagentStart variant); DO NOT copy its incomplete `sed 's/"/\\"/g'` escape pattern — use python3 json.dumps
- @hooks/global/cmm-nudge.sh — reference for project-root walk + md5/md5sum hash fallback
- @tests/test-ctx-search-nudge.sh — delete; @tests/test-ctx-execute-cmm-nudge.sh — template for new test shape
- @.vbw-planning/phases/47-enforcement-audit-ctx-annotation/47-RESEARCH.md — Finding D (schema, prior art, failure modes)
- @.vbw-planning/phases/47-enforcement-audit-ctx-annotation/47-CONTEXT.md — §Phase 45 disposition (consolidate) and §Annotate-nudge cooldown (120s, project-hash-scoped)
- https://code.claude.com/docs/en/hooks — PostToolUse hookSpecificOutput.additionalContext schema

Rationale: self-contained new hook + deletion of predecessor + new test. File-disjoint from Plans 01/02/04. Wave-1 parallelizable. Plan 05 (wave 2) wires this hook into settings-example, setup.sh, agent frontmatter, and CHECKSUMS — keep those changes out of this plan.

## Tasks
1. Create `hooks/global/ctx-annotate-nudge.sh` (chmod +x): shebang, purpose comment, install/register header block with PostToolUse matcher `mcp__context-mode__ctx_(execute|search|index|fetch_and_index)`. Stdin parse via inline python3 that returns base64-encoded (tool_name, tool_input_json, cwd) to tolerate embedded control chars (mirror `ctx-search-nudge.sh` lines 22–57). Early exit 0 on: empty stdin, JSON parse failure, missing tool_name, non-matching tool_name. If decoded tool_input_json contains literal `# ctx-exempt`, exit 0.
2. In the same hook, implement project-root resolution (inline: `git -C "$CWD" rev-parse --show-toplevel` with cwd fallback) and compute `PROJECT_HASH` via `md5 -q || md5sum` fallback. Implement 120s cooldown: `SENTINEL=/tmp/ctx-nudge-${PROJECT_HASH}`; if sentinel exists and `$(date +%s) - $(stat -f %m "$SENTINEL" 2>/dev/null || stat -c %Y "$SENTINEL") -lt 120`, exit 0. If PROJECT_HASH is empty, skip the cooldown check (fail-open to emission).
3. In the same hook, construct the advisory message exactly once as a bash heredoc variable. Call python3 to build and emit the envelope: `python3 -c 'import json,sys; print(json.dumps({"hookSpecificOutput":{"hookEventName":"PostToolUse","additionalContext":sys.argv[1]}}))' "$MSG"`. Then `touch "$SENTINEL" 2>/dev/null || true`. Exit 0.
4. `git rm hooks/global/ctx-search-nudge.sh tests/test-ctx-search-nudge.sh`. Confirm no residual imports/references outside the plan-05 wiring files (grep hooks/ tests/ setup.sh rules/ — Plan 05 will clean those up).
5. Create `tests/test-ctx-annotate-nudge.sh` (chmod +x, plain-bash PASS/FAIL counter, template = tests/test-ctx-execute-cmm-nudge.sh). Implement the 9 cases from `must_haves.artifacts`. For cooldown test, use a tmp directory as PROJECT_HASH source; pre-create the sentinel with current mtime. For JSON-escaping test, feed an `intent` with literal double-quote and newline characters, then assert `python3 -c 'import json,sys; json.loads(sys.stdin.read())'` on the hook's stdout succeeds AND the decoded `additionalContext` substring matches the architect-voice message. Assert that ctx_stats and ctx_batch_execute tools emit nothing on stdout (empty). Exit 0 if all PASS.

## Verification
- `bash hooks/global/ctx-annotate-nudge.sh < /dev/null` exits 0; no stdout.
- Synthetic `{"tool_name":"mcp__context-mode__ctx_execute","tool_input":{"intent":"test"},"cwd":"/tmp"}` (no sentinel) → exit 0; stdout is a single line that parses as JSON and has `.hookSpecificOutput.additionalContext` containing the phrase "state in ONE sentence".
- Same payload immediately again (sentinel now fresh) → exit 0; stdout is empty (cooldown active).
- Synthetic `{"tool_name":"mcp__context-mode__ctx_stats"}` → exit 0; stdout empty.
- Synthetic `{"tool_name":"mcp__context-mode__ctx_search","tool_input":{"queries":["x"]}}` (no sentinel) → exit 0; stdout has additionalContext envelope.
- Payload whose `tool_input.intent` contains `"she said \"hi\"\n\\back"` → hook stdout parses cleanly as JSON (python3 json.loads succeeds), confirming escaping.
- Payload with `tool_input.intent` containing literal `# ctx-exempt` → exit 0; stdout empty.
- `bash tests/test-ctx-annotate-nudge.sh` exits 0; all 9 cases PASS.
- `git status` shows `hooks/global/ctx-search-nudge.sh` and `tests/test-ctx-search-nudge.sh` deleted, new files added.
- `shellcheck hooks/global/ctx-annotate-nudge.sh tests/test-ctx-annotate-nudge.sh` no new errors.
- `grep -rn ctx-search-nudge hooks/ tests/` returns zero matches (settings-example + setup.sh cleanup is Plan 05's job — do not modify those here).

## Success criteria
New PostToolUse hook emits a correctly-escaped `hookSpecificOutput.additionalContext` envelope on qualifying ctx_* calls, respects a 120s project-scoped cooldown, fails-open on all parse errors, honors a `# ctx-exempt` marker, and covers the Phase 45 retrieval-protocol guidance within its message. Predecessor hook and test are deleted atomically in the same plan.

## R01-AMENDMENT (DEV-04)

This section supersedes the final bullet of the `## Verification` block for this plan. The YAML frontmatter above is left frozen as the historical record; this amendment is authoritative going forward.

**Original verification clause (verbatim):**
> "`grep -rn ctx-search-nudge hooks/ tests/` returns zero matches (settings-example + setup.sh cleanup is Plan 05's job — do not modify those here)."

**Why the original clause was over-literal:**
`tests/test-phase-45-bundle-install.sh` retains 8 ctx-search-nudge string references that are *negative retirement assertions* — they assert the nudge is ABSENT from `settings.json`, from `setup.sh` wiring, and from agent frontmatter after install. They are evidence of retirement, not evidence of active wiring. A literal `grep -rn ctx-search-nudge hooks/ tests/` does not distinguish positive assertions ("hook is wired") from negative ones ("hook is not wired"). Deleting those 8 references would weaken the retirement test suite, which is the opposite of the plan's intent.

**Amended verification clause (authoritative):**
> "`grep -rn ctx-search-nudge hooks/ tests/` returns zero matches **except in `tests/test-phase-45-bundle-install.sh`, where retirement-assertion fixtures naming `ctx-search-nudge` are required evidence that the nudge is not re-registered in settings.json, setup.sh wiring, or agent frontmatter after install**. Settings-example + setup.sh cleanup is Plan 05's job — do not modify those here."

**Resolution:** DEV-04 resolved by plan-amendment. No code change required; the 8 references in `tests/test-phase-45-bundle-install.sh` are retained as intended retirement-test coverage.
