---
phase: 47
plan: 02
title: "ctx-execute-enforcer exemption tightening (Finding A)"
wave: 1
depends_on: []
files_modified:
  - hooks/project/ctx-execute-enforcer.sh
  - tests/test-ctx-execute-enforcer.sh
skills_used:
  - codebase-memory-exploring
  - codebase-memory-tracing
  - simplify
must_haves:
  truths:
    - "The unqualified git log/diff/show catch-all (current hooks/project/ctx-execute-enforcer.sh lines 162–166) is REMOVED. Specifically: `git\\ log|git\\ log\\ *|git\\ diff|git\\ diff\\ *` case on line 164 and `git\\ show\\ *` case on line 165 are deleted. Bare `git log`, `git diff`, `git show <sha>` without a bounded flag now fall through to the default block branch."
    - "The bounded git-read exemptions at current lines 129–131 (`git\\ diff\\ --stat*|git\\ log\\ --oneline\\ -*|git\\ show\\ --stat*|git\\ rev-parse\\ *|git\\ rev-list\\ --count*`) are PRESERVED verbatim. Additionally the plan ADDS `git\\ log\\ --name-only*` and `git\\ log\\ --stat*` and `git\\ show\\ --name-only*` to the bounded set so common debugging patterns (scoped commit listings, file-change summaries) still pass."
    - "`echo\\ *|printf\\ *` is REMOVED from the navigation group on current line 142. The rest of line 142 (`date*|uname*|whoami|hostname`) is preserved. `echo`/`printf` calls after this change fall through to the default block branch and must route through ctx_execute."
    - "No other exempt groups are modified: filesystem (lines 133–137), wc/head/tail (line 143), remote (lines 147–149), VBW planning (lines 151–155), version queries (lines 157–160), ctx-exempt marker (line 111), and git write ops (lines 117–124) are preserved verbatim."
    - "Track-hook-blocks.sh instrumentation is ADDED inside each exempt-group fall-through so future audits can quantify reliance. Non-blocking: `bash \"$(dirname \"${BASH_SOURCE[0]}\")/track-hook-blocks.sh\" \"bash-exempt\" \"<group>\" 2>/dev/null || true` inserted once per group before the `exit 0`. Groups labelled: git-write, git-bounded-read, filesystem, navigation, short-reads, remote, vbw-planning, version."
  artifacts:
    - "hooks/project/ctx-execute-enforcer.sh: line 142 split so echo/printf removed; lines 162–166 deleted; bounded git additions inserted after line 131; track-hook-blocks calls inserted per group. Header comment block gets a one-line note: '# 47-02: git log/diff/show bare forms and echo/printf removed from exempt list.' No change to stdin parsing, ctx-exempt handling, or default block message (Plan 46-04 already appended the CMM suffix; this plan does not modify the block message)."
    - "tests/test-ctx-execute-enforcer.sh: add 6 new PASS/FAIL assertions — (a) `git log` (bare) → exit 2 with stderr contains 'BLOCKED: Route this command through ctx_execute'; (b) `git log -20` → exit 2; (c) `git diff HEAD~3` → exit 2; (d) `git show abc123` → exit 2; (e) `echo hello` → exit 2; (f) `printf '%s\\n' foo` → exit 2. Also add 3 PASS assertions for the new bounded forms: `git log --name-only -5` → exit 0; `git log --stat -10` → exit 0; `git show --name-only HEAD` → exit 0. Preserve every existing assertion (git status, git diff --stat, head -N, tail -N, wc, ssh, --version, ctx-exempt marker, etc.)."
  key_links:
    - "Phase 47 success criterion #1 (tightening bare git log/diff/show and removing echo/printf). The plan is the sole owner of ctx-execute-enforcer.sh in wave 1."
    - "No conflict with Plan 46-04's appended CMM suffix: that line remains; this plan only edits exempt case-statements and the header comment."
    - "Simplify review: validate no duplication of track-hook-blocks invocations; validate consolidated bounded-git case statement remains readable."
---

## Objective
Remove the unqualified `git log`/`git diff`/`git show` catch-all and the `echo`/`printf` exemptions from `hooks/project/ctx-execute-enforcer.sh`, preserve the bounded variants, and add `--name-only`/`--stat` bounded forms so legitimate debug commands still pass. Instrument each exempt group with `track-hook-blocks.sh` counters so future audits can quantify reliance.

## Context
- @hooks/project/ctx-execute-enforcer.sh — edit target; lines 115–166 hold the exempt cases. ctx-exempt marker at line 111, block message at 173+ (already gained the Plan 46-04 suffix; preserve).
- @hooks/project/track-hook-blocks.sh — existing counter hook to invoke from exempt branches (non-blocking, exits 0 on any failure)
- @tests/test-ctx-execute-enforcer.sh — extend with 6 new block assertions + 3 new pass assertions
- @.vbw-planning/phases/47-enforcement-audit-ctx-annotation/47-RESEARCH.md — Finding A bypass audit (61 raw Bash calls)
- @.vbw-planning/phases/47-enforcement-audit-ctx-annotation/47-CONTEXT.md — §Deferred Ideas confirms Finding A is research-settled

Rationale: file-disjoint from Plans 01/03/04 (global hooks + rules). Wave-1 parallelizable. No dependency on Plan 46-04 message suffix (that change is orthogonal and already committed).

## Tasks
1. Edit `hooks/project/ctx-execute-enforcer.sh` exempt cases: (a) line 142: replace `echo\ *|printf\ *|date*|uname*|whoami|hostname` with `date*|uname*|whoami|hostname`; (b) delete the entire case block at lines 162–166 (both the `git log|git log *|git diff|git diff *` line and the `git show *` line); (c) in the bounded git-read block (currently lines 127–131), add `|git\ log\ --name-only*|git\ log\ --stat*|git\ show\ --name-only*` to the appropriate case arm. Preserve the header block comments; add a one-line Phase-47 note in the top comment header documenting the change.
2. Insert `track-hook-blocks.sh "bash-exempt" "<group>"` invocations before each `exit 0` in the exempt case blocks: one call per group (git-write, git-bounded-read, filesystem, navigation, short-reads, remote, vbw-planning, version). Form: `bash "$(dirname "${BASH_SOURCE[0]}")/track-hook-blocks.sh" "bash-exempt" "<group>" 2>/dev/null || true`. This must not change the exit-0 outcome. Keep the default-block-path invocation at line 171 unchanged (`"bash"` label, no group).
3. Extend `tests/test-ctx-execute-enforcer.sh`: add the 9 new assertions from `must_haves.artifacts`. Use synthetic stdin JSON `{"tool_name":"Bash","tool_input":{"command":"<cmd>"},"cwd":"<tmp>"}` for each. Assert exit 2 and stderr grep for the block advisory for the 6 block cases; assert exit 0 for the 3 new bounded pass cases. Preserve every existing PASS/FAIL assertion — DO NOT refactor the test file structure. Verify the total PASS count increases by 9.

## Verification
- Synthetic `Bash` input with command `"git log"` → exit 2; stderr contains `BLOCKED: Route this command through ctx_execute`.
- `"git log --oneline -5"` → exit 0.
- `"git log --name-only -10"` → exit 0 (new bounded variant).
- `"git diff"` and `"git diff HEAD~3"` → exit 2; `"git diff --stat"` → exit 0.
- `"git show abc123"` → exit 2; `"git show --stat HEAD"` → exit 0; `"git show --name-only HEAD"` → exit 0.
- `"echo hello world"` → exit 2; `"printf '%s\n' x"` → exit 2; `"date"` → exit 0; `"whoami"` → exit 0.
- `"# ctx-exempt git log"` still exempt (exit 0) — ctx-exempt marker preserved.
- `bash tests/test-ctx-execute-enforcer.sh` exits 0; PASS count increased by 9 vs current baseline.
- `shellcheck hooks/project/ctx-execute-enforcer.sh` reports no new errors.

## Success criteria
Bare `git log`, `git diff`, `git show <ref>` and `echo`/`printf` are hard-blocked and redirected through ctx_execute; bounded variants (`--stat`, `--oneline`, `--name-only`) still pass; every exempt group instruments its usage via `track-hook-blocks.sh`; ctx-exempt bypass marker and block-message wording (including the Plan 46-04 CMM suffix) are preserved byte-for-byte.

## R01-AMENDMENT (DEV-02)

This section supersedes the original `must_haves.artifacts[1]` preservation clause for `tests/test-ctx-execute-enforcer.sh`. The YAML frontmatter above is intentionally left frozen as the historical record; this amendment is authoritative going forward.

**Original clause (verbatim):**
> "Preserve every existing assertion (git status, git diff --stat, head -N, tail -N, wc, ssh, --version, ctx-exempt marker, etc.)."

**Why the original clause was over-literal:**
Plan 47-01 tightened `hooks/project/ctx-execute-enforcer.sh` so that `echo hello` and bare `git diff HEAD~1` no longer match any exempt case and now fall through to the default block branch. Two legacy assertions in `tests/test-ctx-execute-enforcer.sh` asserted the *old* exempt (exit 0) behavior for those exact commands. Preserving them verbatim — as the original clause literally required — would have produced tests that directly contradict the new hook behavior shipped by plan 47-01. Replacing those two assertions with their new blocking (exit 2) form was the only correct path; the rest of the existing assertions were preserved as the clause intended.

**Amended clause (authoritative):**
> "Preserve every existing assertion **except** those asserting behavior that plan 47-01 tightened (`echo hello` and bare `git diff HEAD~1` exemptions) — those must be replaced with assertions of the new blocking behavior. All other pre-existing assertions (git status, git diff --stat, head -N, tail -N, wc, ssh, --version, ctx-exempt marker, etc.) are preserved verbatim."

**Resolution:** DEV-02 resolved by plan-amendment. No code change required; `tests/test-ctx-execute-enforcer.sh` as shipped already matches this amended clause.
