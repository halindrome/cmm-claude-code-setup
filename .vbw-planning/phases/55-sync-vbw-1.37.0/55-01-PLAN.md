---
phase: 55
plan: 01
title: "Sync VBW v1.37.0 agent-body and version-marker changes"
description: "Mirror VBW v1.37.0 PR #627 already_fixed semantics into agents/vbw-debugger.md, bump synced-version marker, add regression test, regenerate checksums."
wave: 1
depends_on: []
skills_used: []
parallelism: serial
estimated_tasks: 5
must_haves:
  - "agents/vbw-debugger.md body contains the upstream v1.37.0 `already_fixed` / accepted-exception semantic block inserted into `## Investigation Protocol`"
  - "All four CMM frontmatter regions in agents/vbw-debugger.md preserved byte-for-byte: (1) `hooks:` block, (2) `permissionMode`/`model`/`memory`/`tools` scalars, (3) `<!-- PROJECT-LEVEL OVERRIDE -->` comment marker, (4) `<!-- cmm-claude-code-setup: Context Mode extensions -->` body block"
  - "No other agents/vbw-*.md file is modified except for the version-marker bookkeeping fields (specifically: vbw-lead.md, vbw-dev.md, vbw-qa.md, vbw-scout.md, vbw-fix-issue.md must remain byte-identical to their state at HEAD of feature/cmm-upstream-sync prior to Phase 55)"
  - "setup.sh records the Synced upstream VBW version as `1.37.0` via a header comment (no prior version constant existed; convention established forward by Phase 55)"
  - "tests/test-agent-hook-enforcement.sh contains a new assertion that vbw-debugger.md body includes the v1.37.0 `already_fixed` block, and the suite exits 0"
  - "CHECKSUMS.sha256 regenerated via scripts/generate-checksums.sh and staged"
  - "Grep audit confirms no stale `named.*must.*team_name` or `team_name.*required.*named` prose in agents/ (PR #633 relaxed-guard verification)"
out_of_scope:
  - "CMM upstream sync (../codebase-memory-mcp) — separate phase"
  - "User VBW plugin cache upgrade — user-driven"
  - "commands/debug.md, commands/fix.md, commands/qa.md, commands/vibe.md, commands/verify.md changes — VBW plugin internals we do not ship"
  - "scripts/lib/active-agent-state.sh, scripts/debug-start-selected-todo.sh, scripts/track-uat-deviations.sh — new plugin-internal scripts"
  - "references/execute-protocol.md, references/phase-detection.md, references/ask-user-question.md — plugin reference docs"
  - "All VBW upstream bats/test files under tests/ and testing/ — plugin-internal"
  - "Caveman framework adoption — explicitly deferred since Phase 49"
context:
  - "@.vbw-planning/phases/55-sync-vbw-1.37.0/55-RESEARCH.md — authoritative impact map; Task 1 mirror block is quoted verbatim there"
  - "@agents/vbw-debugger.md — target file for body insertion; CMM frontmatter must be preserved"
  - "@setup.sh — version marker bump target"
  - "@tests/test-agent-hook-enforcement.sh — pattern reference for new body-assertion line (follows Phase 54 Task 5 pattern)"
  - "@scripts/generate-checksums.sh — regen script invoked by Task 5; Phase 51 added agents/ coverage"
---

# Objective
Mirror the only agent-body-relevant change in the upstream VBW v1.36.2 → v1.37.0 delta (PR #627's `already_fixed` / accepted-exception semantic block into `agents/vbw-debugger.md`), update the synced-version marker in `setup.sh`, add a regression assertion, regenerate checksums, and confirm by grep audit that no other agent override drifted against PR #633's relaxed named-subagent rule. All other upstream changes are VBW-plugin-internal and require no local action per Scout's classification.

# Tasks

## Task 1: Mirror `already_fixed` / accepted-exception block into agents/vbw-debugger.md
files:
  - agents/vbw-debugger.md
description: |
  In the `## Investigation Protocol` section of `agents/vbw-debugger.md`, insert the upstream
  v1.37.0 PR #627 body block immediately before the line `> As teammate: use SendMessage instead
  of final report document.` (the insertion point matches upstream line 33 anchor in v1.37.0).

  Paste the following text VERBATIM from Scout's research (RESEARCH.md PR #627 diff, lines 47-64):

      Historical accepted exception/backlog markers are historical phase/round waivers and backlog
      pointers, not proof that the underlying issue is fixed. Known-issue sources include
      `[KNOWN-ISSUE]`, `Disposition: accepted-process-exception`, and `known_issue_signature.disposition`.
      UAT-deviation sources include `[UAT-DEVIATION]`, `source: "uat-deviation"`, an `uat_deviation`
      object, and the phrase `Accepted UAT summary deviation`. When the user selects the item with
      `/vbw:debug <todo-number>`, treat it as an active remediation request. Do not set or accept
      `already_fixed` solely because source metadata says accepted, non-blocking, UAT deviation,
      process exception, or backlog. `already_fixed` requires fresh current evidence that the underlying
      issue no longer reproduces or the current branch already contains a real fix. If still actionable,
      use `resolution_observation=needs_change`; if impossible or unsafe without more input, report an
      explicit blocker.

  Do NOT alter any other line of the file. Do NOT touch the YAML frontmatter, the `hooks:` block,
  the `<!-- PROJECT-LEVEL OVERRIDE -->` comment, or the `<!-- cmm-claude-code-setup: Context Mode extensions -->`
  trailing body block.
acceptance:
  - "`grep -c 'already_fixed.*fresh current evidence' agents/vbw-debugger.md` returns exactly 1"
  - "`grep -c 'known_issue_signature.disposition' agents/vbw-debugger.md` returns exactly 1"
  - "`grep -c 'Accepted UAT summary deviation' agents/vbw-debugger.md` returns exactly 1"
  - "`grep -c '^hooks:' agents/vbw-debugger.md` returns exactly 1 (CMM frontmatter intact)"
  - "`grep -c 'PROJECT-LEVEL OVERRIDE' agents/vbw-debugger.md` returns exactly 1"
  - "`grep -c 'cmm-claude-code-setup: Context Mode extensions' agents/vbw-debugger.md` returns exactly 1"
  - "Inserted block sits inside `## Investigation Protocol` and immediately precedes the `> As teammate:` blockquote"
  - "Single atomic commit: `feat(agents): mirror v1.37.0 already_fixed semantics into vbw-debugger`"

## Task 2: Audit other agent overrides for PR #633 stale-prose drift
files:
  - agents/vbw-lead.md
  - agents/vbw-dev.md
  - agents/vbw-qa.md
  - agents/vbw-scout.md
  - agents/vbw-fix-issue.md
description: |
  PR #633 (named-non-team subagent guard relaxation) does not require any local agent-body change
  per Scout's grep audit, but the rule must be re-verified before this phase ships. Run:

      grep -rn 'named.*must.*team_name\|team_name.*required.*named' agents/

  Expected result: empty output. If any lines are found, edit the affected file to match the
  relaxed rule ("named subagents are permitted without team_name; team_name is required only in
  true execute-team mode"). If no lines are found, this task closes as a no-op verification with
  no file edits and no commit — record the empty-grep result in the SUMMARY.md notes for QA.

  This task explicitly must NOT modify any other content of the listed agent files. Those five
  files must remain byte-identical to their state at the start of Phase 55 unless the grep above
  surfaces a real hit.
acceptance:
  - "`grep -rn 'named.*must.*team_name\\|team_name.*required.*named' agents/` returns no matches"
  - "If no matches: no file edits, no commit, SUMMARY.md records `Task 2: verified clean (no drift)`"
  - "If matches found: one atomic commit `fix(agents): relax named-subagent prose per VBW PR #633` containing only the minimal correction"
  - "vbw-lead.md, vbw-dev.md, vbw-qa.md, vbw-scout.md, vbw-fix-issue.md unchanged unless grep surfaces a hit"

## Task 3: Bump synced VBW version marker in setup.sh to 1.37.0
files:
  - setup.sh
description: |
  Locate the line in `setup.sh` that records the synced upstream VBW version (the constant or
  header comment currently containing `1.36.2`). Replace `1.36.2` with `1.37.0`. Do NOT introduce
  a new variable name; reuse whatever marker convention is already in place from the Phase 54
  v1.36.2 bump. If `setup.sh` records the marker in multiple legitimate places (e.g., a header
  comment AND a constant), update all active occurrences. Do not modify any line that is purely
  changelog history.

  Preflight grep: `grep -n '1\.36\.2' setup.sh` — note all hits. Update each active occurrence
  that represents the "currently synced VBW version" and leave any historical references alone.
acceptance:
  - "`grep -c '1\\.37\\.0' setup.sh` returns at least 1"
  - "`grep -n '1\\.36\\.2' setup.sh` returns only changelog/comment-history lines (no active marker)"
  - "setup.sh remains executable and `bash -n setup.sh` returns 0"
  - "Single atomic commit: `chore(setup): bump synced VBW version marker to 1.37.0`"

## Task 4: Add v1.37.0 body assertion to tests/test-agent-hook-enforcement.sh
files:
  - tests/test-agent-hook-enforcement.sh
description: |
  Following the Phase 54 Task 5 pattern (which added body-content assertions for agent overrides),
  add a new assertion that `agents/vbw-debugger.md` contains the v1.37.0 `already_fixed` keyword.
  The assertion line should follow the existing grep-based style used elsewhere in the file —
  reuse the `AGENTS_DIR` variable (or whatever variable already references the agents directory
  in this test) and the same pass/fail-counter pattern as the surrounding assertions.

  Suggested assertion line (adapt to match the file's existing style, including helper-function
  names and counter variables):

      grep -q "already_fixed.*fresh current evidence" "${AGENTS_DIR}/vbw-debugger.md" \
        || fail "vbw-debugger.md missing v1.37.0 already_fixed semantic block"

  Run the suite locally after editing: `bash tests/test-agent-hook-enforcement.sh` — it must exit 0.
acceptance:
  - "tests/test-agent-hook-enforcement.sh contains a new line asserting `already_fixed.*fresh current evidence` in vbw-debugger.md"
  - "`bash tests/test-agent-hook-enforcement.sh` exits 0"
  - "New assertion follows the existing file's style (same helper / counter conventions)"
  - "Single atomic commit: `test(agents): assert v1.37.0 already_fixed block in vbw-debugger`"

## Task 5: Regenerate CHECKSUMS.sha256 and verify
files:
  - CHECKSUMS.sha256
description: |
  After Tasks 1, 3, and 4 land (which modify `agents/vbw-debugger.md`, `setup.sh`, and
  `tests/test-agent-hook-enforcement.sh`), run `bash scripts/generate-checksums.sh` to regenerate
  `CHECKSUMS.sha256`. Stage the regenerated file. The regen script already covers `agents/`
  (added in Phase 51) so no script edit is required.

  Confirm idempotency: re-run `bash scripts/generate-checksums.sh`; the file should be byte-stable
  between consecutive runs.
acceptance:
  - "`bash scripts/generate-checksums.sh` completes with exit 0"
  - "`CHECKSUMS.sha256` shows updated hashes for `agents/vbw-debugger.md`, `setup.sh`, `tests/test-agent-hook-enforcement.sh`"
  - "Second consecutive run of `scripts/generate-checksums.sh` produces zero diff in CHECKSUMS.sha256"
  - "Single atomic commit: `chore(checksums): regenerate after VBW v1.37.0 sync`"

# Verification (plan-level)
- "`grep -q 'already_fixed.*fresh current evidence' agents/vbw-debugger.md && echo PASS`"
- "`grep -c '^hooks:' agents/vbw-debugger.md` returns 1 (CMM frontmatter preserved)"
- "`grep -c 'cmm-claude-code-setup: Context Mode extensions' agents/vbw-debugger.md` returns 1"
- "`grep -rn 'named.*must.*team_name\\|team_name.*required.*named' agents/` is empty"
- "`grep '1\\.37\\.0' setup.sh` finds active marker; `grep '1\\.36\\.2' setup.sh` finds only history (or empty)"
- "`bash tests/test-agent-hook-enforcement.sh` exits 0"
- "`bash scripts/generate-checksums.sh` is idempotent on second invocation"
- "`git diff --stat main...HEAD -- agents/` shows only `vbw-debugger.md` modified (and possibly nothing else if Task 2 was a no-op)"
- "Upstream parity: `diff <(git -C ../vibe-better-with-claude-code-vbw show v1.37.0:agents/vbw-debugger.md) <(filtered local vbw-debugger.md)` shows only CMM-marker additions as local extras"

# Success Criteria
- All five tasks complete with their commits landed on the feature branch
- Phase 55 SUMMARY.md records commit hashes for each task
- Plan-level verification block above passes end-to-end
- No file outside the declared `files:` lists of Tasks 1–5 is modified by this plan

## Resolved Deviations
- **DEVN-04**: resolved by this amendment — original Task 3 must_have wording (`"replacing the prior 1.36.2 marker"`) corrected to reflect the established-forward convention. Verified at commit `70b24f1` that no prior `1.36.2` marker existed in setup.sh (`git show 70b24f1:setup.sh | grep -c '1.36.2'` = 0). Phase 55 established the synced-VBW-version header comment forward; future remediation/QA rounds verify against this corrected contract.
