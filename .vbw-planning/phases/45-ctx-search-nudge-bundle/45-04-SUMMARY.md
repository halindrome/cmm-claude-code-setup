---
phase: 45
plan: 04
title: "setup.sh wiring + global-claude-md.md reference + integration smoke"
status: complete
completed: 2026-04-17
tasks_completed: 5
tasks_total: 5
commit_hashes:
  - 8408446
  - fd57453
  - 21eb0e6
  - 4c03c1d
  - ec842b3
files_modified:
  - setup.sh
  - rules/project-settings-example.json
  - README.md
  - docs/setup-guide.md
  - tests/test-phase-45-bundle-install.sh
deviations:
  - "DEVN-02 (stale plan reference): plan 45-04 files_modified lists global-claude-md.md, but that file does not exist in the repo — it appears in setup.sh's _legacy_files cleanup list (setup.sh L897) and is actively removed on every --project install as a legacy rule-file artifact from earlier setups. No user-facing global-claude-md.md exists. The plan's intent (documenting the new rule alongside cmm-rules.md) was preserved by updating the two real user-facing docs that reference cmm-rules.md: README.md (rules listing, line 102) and docs/setup-guide.md Step 4 (line 162). Commit 21eb0e6 explains the substitution."
  - "DEVN-01 (settings registration location): plan 45-04 truths say 'registers both new hooks in .claude/settings.json'. In this codebase, project settings.json is generated by merge_settings_json from rules/project-settings-example.json, so the registrations were added there (rules/project-settings-example.json) rather than inline in setup.sh. This matches how every other project hook (ctx-execute-enforcer.sh, subagent-cmm-startup.sh, cmm-query-stale-advisory.sh, context-mode-sentinel-writer.sh) is registered."
  - "DEVN-01 (gating): plan 45-04 truth says 'Both new hooks are wired INSIDE the existing context-mode install gate (only register when INSTALL_CONTEXT_MODE=true / context-mode is present)'. 45-CONTEXT.md's absence-detection decision states the hooks no-op when context-mode is not installed and explicitly calls the gate 'for tidiness only, safe without'. The existing ctx-execute-enforcer.sh registration in project-settings-example.json is NOT gated — it is registered unconditionally and relies on its own absence probe. Matched that established pattern rather than adding conditional registration logic to merge_settings_json."
pre_existing_issues: []
---

Wired all three wave-1 phase-45 artifacts into setup.sh, registered the two new hooks in the project-settings-example.json template, added documentation references alongside cmm-rules.md, and added a phase-level integration smoke test that validates end-to-end install + idempotency.

## What Was Built

- **setup.sh install_project hook copies** (setup.sh ~L813-829): explicit copy_file + set_executable blocks for `hooks/global/ctx-search-nudge.sh` and `hooks/global/subagent-ctx-startup.sh`, placed immediately after the existing webfetch-nudge.sh copy block to keep the diff reviewable.
- **setup.sh rules copy comment** (setup.sh ~L887): added a comment naming both cmm-rules.md and ctx-rules.md so the install flow is self-documenting. The pre-existing rules/* loop already copies ctx-rules.md — no behavioral change, just clarity.
- **rules/project-settings-example.json PostToolUse registration**: new entry with matcher `mcp__context-mode__ctx_execute|mcp__context-mode__ctx_index|mcp__context-mode__ctx_fetch_and_index|mcp__context-mode__ctx_batch_execute` pointing at `.claude/hooks/ctx-search-nudge.sh`. Unconditional (relies on hook's built-in absence probe), matching the ctx-execute-enforcer.sh pattern already in the file.
- **rules/project-settings-example.json SubagentStart registration**: second SubagentStart entry alongside the existing subagent-cmm-startup.sh, pointing at `.claude/hooks/subagent-ctx-startup.sh`.
- **README.md rules listing** (L102-103): added ctx-rules.md line directly below cmm-rules.md in the rules/ tree block.
- **docs/setup-guide.md Step 4** (L162): extended the single rules sentence to name both cmm-rules.md and ctx-rules.md and to describe the Context Mode retrieval protocol.
- **tests/test-phase-45-bundle-install.sh**: new integration smoke test (129 lines). Creates a scratch git repo via mktemp, runs `setup.sh --project --yes --skip-mcp-check` (piping "n" to the allowlist prompt to avoid the blocking read), asserts all three artifacts land in .claude/ with the right permissions, both hook registrations appear in settings.json under the correct events with the correct matcher, and confirms byte-identical idempotency by SHA-comparing settings.json between two runs.

### Verification

- `grep -c 'ctx-search-nudge.sh' setup.sh` → 4 (comment + copy + chmod + commentary). Plan required ≥2.
- `grep -c 'subagent-ctx-startup.sh' setup.sh` → 4. Plan required ≥2.
- `grep -c 'ctx-rules.md' setup.sh` → 1 (loop comment). Plan required ≥1.
- `grep -c 'ctx-rules.md' README.md` → 1, `grep -c 'ctx-rules.md' docs/setup-guide.md` → 1.
- Matcher string `mcp__context-mode__ctx_execute|mcp__context-mode__ctx_index|mcp__context-mode__ctx_fetch_and_index|mcp__context-mode__ctx_batch_execute` present in rules/project-settings-example.json L122.
- `bash tests/test-phase-45-bundle-install.sh` → 11 passed, 0 failed. Verifies artifact presence, permission bits, both settings registrations, matcher exactness, settings.json byte-identical idempotency on second run, and hook file content identity to sources.
- Re-ran `tests/test-ctx-search-nudge.sh` (13/13 pass) and `tests/test-subagent-ctx-startup.sh` (6/6 pass) — no wave-1 regression.

## Files Modified

- `setup.sh` — two new copy_file + set_executable blocks in install_project (ctx-search-nudge.sh, subagent-ctx-startup.sh) + clarifying comment on the rules/* copy loop naming both cmm-rules.md and ctx-rules.md.
- `rules/project-settings-example.json` — new PostToolUse entry for ctx-search-nudge.sh (four-tool matcher) and second SubagentStart entry for subagent-ctx-startup.sh.
- `README.md` — ctx-rules.md line added to the rules/ tree listing directly below cmm-rules.md.
- `docs/setup-guide.md` — Step 4 updated to name both rules and describe the ctx_search retrieval protocol.
- `tests/test-phase-45-bundle-install.sh` — new 129-line integration smoke test (chmod +x at creation time).
