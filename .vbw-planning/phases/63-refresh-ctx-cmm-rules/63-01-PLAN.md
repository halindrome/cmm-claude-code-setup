---
phase: 63
plan: "01"
title: "Refresh ctx-rules and cmm-rules Source Files for Running Tool Versions"
wave: 1
depends_on: []
skills_used:
  - .claude/skills/ctx-rules/SKILL.md
  - .claude/skills/cmm-rules/SKILL.md
must_haves:
  truths:
    - "grep 'project:' rules/ctx-rules.md returns at least 1 match (project: parameter documented in ctx_search row)"
    - "grep '\"global\"' rules/ctx-rules.md returns at least 1 match (global value form documented)"
    - "grep 'sort.*timeline\\|timeline.*sort' rules/ctx-rules.md returns at least 1 match"
    - "grep 'contentType' rules/ctx-rules.md returns at least 1 match"
    - "grep 'v0.7.0' rules/cmm-rules.md returns at least 1 match (version label bumped)"
    - "grep 'qualified_name' rules/cmm-rules.md returns at least 1 match (trace_path fallback documented)"
    - "grep 'LSP' rules/cmm-rules.md returns at least 1 match (call graph accuracy note added)"
    - "diff rules/ctx-rules.md .claude/rules/ctx-rules.md returns no diff (installed copy matches source)"
    - "diff rules/cmm-rules.md .claude/rules/cmm-rules.md returns no diff (installed copy matches source)"
    - "shasum -a 256 --check CHECKSUMS.sha256 exits 0 for rules/ctx-rules.md and rules/cmm-rules.md entries"
    - "grep 'v0.6.1' rules/cmm-rules.md returns 0 matches (stale label fully replaced)"
    - "No .claude/rules/ files edited directly (source-only constraint)"
    - "git diff --name-only contains no files under hooks/ agents/ or setup.sh (scope confined to rules/*.md, CHECKSUMS.sha256, tests/)"
  artifacts:
    - path: "rules/ctx-rules.md"
      content: "ctx_search table row enriched with project: parameter (omit/global/<absolute-path>); sort: timeline and contentType: code|prose noted; all other content verbatim unchanged"
    - path: "rules/cmm-rules.md"
      content: "Behavior notes label reads v0.7.0; trace_path qualified_name fallback note added; LSP-resolved call graph accuracy note added; three pre-existing notes retained"
    - path: "CHECKSUMS.sha256"
      content: "sha256 entries for rules/ctx-rules.md and rules/cmm-rules.md regenerated to match edited sources"
    - path: "tests/test-phase-63-rule-refresh.sh"
      content: "assertions: ctx_search row has project:, sort: timeline, contentType documented; cmm-rules has v0.7.0, qualified_name, LSP; installed .claude/rules/ copies match sources; CHECKSUMS pass"
  key_links:
    - "rules/ctx-rules.md edits must propagate identically to .claude/rules/ctx-rules.md via setup.sh --project"
    - "rules/cmm-rules.md edits must propagate identically to .claude/rules/cmm-rules.md via setup.sh --project"
    - "CHECKSUMS.sha256 lines 34-35 cover rules/cmm-rules.md and rules/ctx-rules.md — both must be regenerated after source edits"
---

## Objective

Bring the two source rule files in sync with the upstream tool versions actually running
(context-mode 1.0.161; codebase-memory-mcp 0.7.0). Every existing fact in both files is
still accurate — these are additive edits only, surfacing new capabilities to agents and
subagents. After editing sources, propagate via `setup.sh --project`, regenerate the
affected CHECKSUMS.sha256 entries, and add a test to lock in the assertions.

## Context

- `@rules/ctx-rules.md` — source for context-mode tool guidance; ctx_search row missing project:, sort:, contentType
- `@rules/cmm-rules.md` — source for CMM tool guidance; behavior-notes label stale (v0.6.1+), missing trace_path fallback + LSP accuracy note
- `@.vbw-planning/phases/63-refresh-ctx-cmm-rules/63-RESEARCH.md` — authoritative exact-edits specification; treat as ground truth
- `@CHECKSUMS.sha256` — tracks rules/ctx-rules.md (line 35) and rules/cmm-rules.md (line 34); must be regenerated after source changes
- `@.claude/skills/ctx-rules/SKILL.md` — context-mode retrieval protocol; governs ctx_search usage in agents
- `@.claude/skills/cmm-rules/SKILL.md` — CMM navigation protocol; governs orient-first pattern in agents

Hard constraints (from RESEARCH.md and CLAUDE.md):
- Edit SOURCE files in `rules/` only; NEVER edit `.claude/rules/*` (installed artifacts from setup.sh)
- No tool upgrades; no changes to setup.sh logic, hooks/, or agents/
- No invented tool-table rows; tool inventory in both files is unchanged from the running tool surface
- Feature branch off `develop`; one atomic commit per task; conventional commit format

## Tasks

### T1 — Create feature branch off develop

Checkout or create the feature branch `feature/phase-63-refresh-ctx-cmm-rules` from `develop`.
The repo is currently on `main`; do NOT commit directly to main or develop.

Commands:
```
git fetch origin
git checkout develop
git pull origin develop
git checkout -b feature/phase-63-refresh-ctx-cmm-rules
```

Acceptance criteria:
- `git branch --show-current` returns `feature/phase-63-refresh-ctx-cmm-rules`
- Branch is based on develop (not main)

Commit: none (branch creation only)

### T2 — Edit rules/ctx-rules.md: enrich ctx_search row

Read `rules/ctx-rules.md` in full (to capture exact current bytes for Edit). Make the following
additive changes to the `ctx_search` table row and surrounding prose:

1. Enrich the `ctx_search` table row description with the `project:` parameter: omit = current
   session's project; `"global"` = all indexed projects; `"<absolute-path>"` = a specific project.
2. After (or within) the table, add a sentence noting `sort: "timeline"` (chronological recall
   across prior sessions and auto-memory; useful after `/compact` or `--resume`) and the
   `contentType: "code"|"prose"` filter.

Leave EVERYTHING else verbatim: INTENT_SEARCH_THRESHOLD = 5_000, PostToolUse-always-on description,
concurrency guidance, all anti-patterns. Do not reformat, reorder, or rephrase any existing content.

Files to modify:
- `rules/ctx-rules.md`

Acceptance criteria:
- `grep 'project:' rules/ctx-rules.md` returns at least 1 match
- `grep '"global"' rules/ctx-rules.md` returns at least 1 match
- `grep 'timeline' rules/ctx-rules.md` returns at least 1 match
- `grep 'contentType' rules/ctx-rules.md` returns at least 1 match
- `grep 'INTENT_SEARCH_THRESHOLD' rules/ctx-rules.md` still returns exactly 1 match (unchanged)
- `grep 'PostToolUse' rules/ctx-rules.md` still returns matches (section unchanged)
- `wc -l rules/ctx-rules.md` increases by at most 8 lines relative to baseline (additive only)

Commit: `docs(rules): enrich ctx_search row with project:, sort:timeline, contentType params`

### T3 — Edit rules/cmm-rules.md: bump to v0.7.0 and add behavior notes

Read `rules/cmm-rules.md` in full (to capture exact current bytes for Edit). Make the following
additive changes:

1. Bump the section heading "Behavior notes (upstream v0.6.1+)" to read "Behavior notes (upstream v0.7.0)".
2. Add a behavior note that `trace_path` falls back to a `qualified_name` lookup when the bare
   `function_name` does not resolve (CMM commit 9818730).
3. Add one line stating the call graph is now LSP-resolved (accurate) as of v0.7.0 — `trace_path`
   and `search_graph` CALLS edges resolve to the right callee (hybrid LSP across six languages);
   prefer them over manual grep.
4. Retain the three pre-existing notes exactly as written (200-row search_graph cap, search_code
   multi-word→regex, list_projects /tmp/ visibility).

Files to modify:
- `rules/cmm-rules.md`

Acceptance criteria:
- `grep 'v0.7.0' rules/cmm-rules.md` returns at least 1 match (section label updated)
- `grep 'v0.6.1' rules/cmm-rules.md` returns 0 matches (stale label fully replaced)
- `grep 'qualified_name' rules/cmm-rules.md` returns at least 1 match
- `grep 'LSP' rules/cmm-rules.md` returns at least 1 match
- `grep '200' rules/cmm-rules.md` still returns a match (200-row cap note retained)
- `grep 'multi-word\|multi.word\|foo.*bar\|regex' rules/cmm-rules.md` still returns a match (search_code note retained)
- `grep '/tmp/' rules/cmm-rules.md` still returns a match (list_projects /tmp note retained)
- No tool-table rows added or removed (tool inventory identical to baseline)

Commit: `docs(rules): bump cmm-rules to v0.7.0, add trace_path fallback and LSP call-graph note`

### T4 — Propagate via setup.sh --project and regenerate CHECKSUMS.sha256

After the two source edits are committed on the feature branch:

1. Run `bash setup.sh --project --force` (from the repo root) to propagate
   the edited sources into `.claude/rules/`.
   # --force required in non-interactive mode: copy_file skips overwriting differing files when tty=false
   # DEV-01: resolved-by-amendment — command updated to include --force
2. Verify the installed copies match the sources:
   - `diff rules/ctx-rules.md .claude/rules/ctx-rules.md` must exit 0 (no diff)
   - `diff rules/cmm-rules.md .claude/rules/cmm-rules.md` must exit 0 (no diff)
3. Regenerate the CHECKSUMS.sha256 entries for the two modified rule files. Inspect the current
   CHECKSUMS.sha256 to confirm the checksum command (`sha256sum` or `shasum -a 256`). Update only
   the lines for `rules/ctx-rules.md` and `rules/cmm-rules.md` — do NOT alter entries for any
   other file.
4. Verify: `shasum -a 256 --check CHECKSUMS.sha256` (or `sha256sum --check`) exits 0 for all entries.

NOTE: setup.sh may also install/update files in `.claude/` beyond just rules/ — do not commit any
`.claude/` changes; only commit `CHECKSUMS.sha256`. The `.claude/` directory is not tracked in git
in this repo (confirm with `git status` before staging).

Files to modify:
- `CHECKSUMS.sha256`

Acceptance criteria:
- `diff rules/ctx-rules.md .claude/rules/ctx-rules.md` exits 0
- `diff rules/cmm-rules.md .claude/rules/cmm-rules.md` exits 0
- `shasum -a 256 --check CHECKSUMS.sha256` exits 0 (all entries pass)
- `git diff --name-only` shows only `CHECKSUMS.sha256` staged in this task (rules/ edits already committed in T2/T3)

Commit: `chore(rules): regenerate CHECKSUMS.sha256 for ctx-rules and cmm-rules`

### T5 — Add test-phase-63-rule-refresh.sh and open PR

1. Write `tests/test-phase-63-rule-refresh.sh` following the style of `tests/test-phase-56-cmm-sync.sh`
   or `tests/test-phase-62-mcp-grants.sh`. The test must assert:

   a. `rules/ctx-rules.md` ctx_search row documents `project:` with the three value forms (omit,
      `"global"`, `<absolute-path>`).
   b. `rules/ctx-rules.md` mentions `sort: "timeline"` and `contentType`.
   c. `rules/cmm-rules.md` behavior-notes label contains `v0.7.0` (not `v0.6.1`).
   d. `rules/cmm-rules.md` documents `qualified_name` (trace_path fallback).
   e. `rules/cmm-rules.md` mentions `LSP` (call graph accuracy).
   f. `diff rules/ctx-rules.md .claude/rules/ctx-rules.md` exits 0 (propagation check).
   g. `diff rules/cmm-rules.md .claude/rules/cmm-rules.md` exits 0 (propagation check).

   The test script must be executable (`chmod +x`) and exit 0 on pass, non-zero on failure.

2. Push the feature branch and open a PR targeting `develop` (NOT main). PR title:
   `docs(rules): refresh ctx-rules and cmm-rules for context-mode 1.0.161 / CMM 0.7.0`
   # DEV-02: process-exception — push/PR deferred; CLAUDE.md forbids git push without explicit user authorization.
   # Branch is complete locally; user must explicitly request push to proceed.

Files to modify:
- `tests/test-phase-63-rule-refresh.sh` (new file)

Acceptance criteria:
- `bash tests/test-phase-63-rule-refresh.sh` exits 0 with all assertions passing
- PR is open targeting `develop`
- PR description references Phase 63 and lists the two source files changed
- No files under `hooks/`, `agents/`, or `setup.sh` appear in the diff

Commit: `test(rules): add phase-63 rule-refresh assertions`

## Verification

```bash
# T2: ctx-rules enrichment
grep 'project:' rules/ctx-rules.md
grep '"global"' rules/ctx-rules.md
grep 'timeline' rules/ctx-rules.md
grep 'contentType' rules/ctx-rules.md
grep 'INTENT_SEARCH_THRESHOLD' rules/ctx-rules.md  # must still be present

# T3: cmm-rules bump
grep 'v0.7.0' rules/cmm-rules.md
grep 'v0.6.1' rules/cmm-rules.md  # must return 0 matches
grep 'qualified_name' rules/cmm-rules.md
grep 'LSP' rules/cmm-rules.md
grep '200' rules/cmm-rules.md        # 200-row cap note retained
grep '/tmp/' rules/cmm-rules.md      # list_projects /tmp note retained

# T4: propagation and checksums
diff rules/ctx-rules.md .claude/rules/ctx-rules.md
diff rules/cmm-rules.md .claude/rules/cmm-rules.md
shasum -a 256 --check CHECKSUMS.sha256

# T5: test suite
bash tests/test-phase-63-rule-refresh.sh

# Scope guard: no hooks/agents/setup.sh in diff
git diff develop...HEAD --name-only | grep -v '^rules/' | grep -v '^CHECKSUMS' | grep -v '^tests/'
# above should return empty (or only .vbw-planning/ planning artifacts)
```

## Success Criteria

- `rules/ctx-rules.md` ctx_search row documents the `project:` parameter with all three value forms
  (omit / `"global"` / `"<absolute-path>"`), and the file notes `sort: "timeline"` and
  `contentType: "code"|"prose"`. No other content in the file is altered.
- `rules/cmm-rules.md` behavior-notes label reads v0.7.0; a note documents trace_path →
  qualified_name fallback; a note documents the LSP-resolved (accurate) call graph as of 0.7.0;
  the three pre-existing notes are retained and still correct.
- No upgrade performed; setup.sh logic, hooks/, and agents/ are unchanged.
- `setup.sh --project` re-run propagates the edited sources and the installed `.claude/rules/ctx-rules.md` /
  `.claude/rules/cmm-rules.md` byte-match the `rules/` sources (diff exits 0).
- CHECKSUMS.sha256 passes for all entries (`shasum -a 256 --check CHECKSUMS.sha256` exits 0).
- No fabricated tool-table rows; tool inventory in both files is unchanged from the running tool surface.
- `bash tests/test-phase-63-rule-refresh.sh` exits 0.
- PR open targeting `develop`; standard 2–4 QA rounds to follow (diff touches rules/ and setup.sh output).
